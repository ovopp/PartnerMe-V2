import { test } from "../../index";

const testFunc = jest.fn(x => x == 1);

describe("testfunction()", () => {
    it("should return the value of the inputed function given an input value of 1", () => {
	
        expect(test()).toBeTruthy();
    });
});

